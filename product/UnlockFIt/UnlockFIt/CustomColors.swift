//
//  CustomColors.swift
//  UnlockFIt
//
//  Created by woozy on 05/12/2024.
//


import SwiftUI

struct CustomColors {
    // Fitness Rings - Primary
    static let ringRed = Color(red: 1.0, green: 0.26, blue: 0.33) // Hex: #FF4355
    static let ringGreen = Color(red: 0.4, green: 0.85, blue: 0.4) // Hex: #66D966
    static let ringBlue = Color(red: 0.3, green: 0.63, blue: 1.0) // Hex: #4FA0FF

    // Fitness Rings - Secondary (Much Darker)
    static let ringRed2 = Color(red: 0.4, green: 0.0, blue: 0.1) // Hex: #660019
    static let ringGreen2 = Color(red: 0.05, green: 0.2, blue: 0.05) // Hex: #0D330D
    static let ringBlue2 = Color(red: 0.0, green: 0.15, blue: 0.4) // Hex: #002666

    // Nike Run Club Neon
    static let neonYellow = Color(red: 0.89, green: 1.0, blue: 0.22) // Hex: #E3FF38
    static let neonYellow2 = Color(red: 0.4, green: 0.45, blue: 0.1) // Hex: #66731A

    // Badges - Primary
    static let bronze = Color(red: 0.8, green: 0.5, blue: 0.2) // Hex: #CC8040
    static let silver = Color(red: 0.75, green: 0.75, blue: 0.75) // Hex: #BFBFBF
    static let gold = Color(red: 1.0, green: 0.84, blue: 0.0) // Hex: #FFD700
    static let platinum = Color(red: 0.9, green: 0.9, blue: 0.93) // Hex: #E6E6ED

    // Badges - Secondary (Much Darker)
    static let bronze2 = Color(red: 0.4, green: 0.2, blue: 0.0) // Hex: #663300
    static let silver2 = Color(red: 0.3, green: 0.3, blue: 0.3) // Hex: #4D4D4D
    static let gold2 = Color(red: 0.5, green: 0.35, blue: 0.0) // Hex: #805900
    static let platinum2 = Color(red: 0.5, green: 0.5, blue: 0.55) // Hex: #808089
    
    static let lightGray = Color(white: 0.6)
}
